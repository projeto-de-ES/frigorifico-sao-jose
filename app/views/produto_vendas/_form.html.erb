<p style="color: #cc0000" id="notice"><%= notice %></p>
<%= form_with(model: [@venda, produto_venda], local: true) do |form| %>
  <% if produto_venda.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(produto_venda.errors.count, "error") %> prohibited this produto_venda from being saved:</h2>

      <ul>
        <% produto_venda.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row form-group">
    <div class="col-sm-4">
      <%= form.label :produto_id %>
      <%= form.select :produto_id, @produtos, {include: true}, class: "form-control"%>
    </div>

    <div class="col-sm-4">
      <%= form.label :qtd_produtos %>
      <%= form.text_field :qtd_produtos, class: "form-control" %>
    </div>
  </div>

  

  <div class="actions">
    <%= form.submit "Adicionar", class: "btn btn-success"%>
  </div>
<% end %>
